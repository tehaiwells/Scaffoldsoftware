# Set these in your shell; no dotenv dependency is required.
PORT=3000
# 127.0.0.1 keeps the app private to this PC. Set 0.0.0.0 to share it with other devices on your local network.
HOST=127.0.0.1
DATABASE_PATH=./data/scaffold.sqlite
# Only enable behind HTTPS when deploying:
COOKIE_SECURE=false
